import { StationFilterPipePipe } from './station-filter-pipe.pipe';

describe('StationFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new StationFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
